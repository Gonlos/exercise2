version: "3"
services:
  exercise1:
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV}
    build: .
    ports:
      - "9001:9001"
  messageapp:
    image: "cabify/backend-bootcamp-messageapp:latest"
    ports:
      - "3000:3000"
